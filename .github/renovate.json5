{
  extends: ['github>seek-oss/rynovate'],
  packageRules: [
    {
      matchManagers: ['npm'],
      matchPackageNames: [
        // `node-fetch@3` is ESM-only
        '@types/node-fetch',
        'node-fetch',
      ],
      matchUpdateTypes: ['major'],

      enabled: false,
    },
  ],
}
