{
  extends: ['github>seek-oss/rynovate'],
  packageRules: [
    {
      matchManagers: ['npm'],
      matchPackageNames: [
        // ESM only
        '@types/node-fetch',
        'node-fetch',
        'query-string'
      ],
      matchUpdateTypes: ['major'],

      enabled: false,
    },
  ],
}
